{% extends 'base.html.twig' %}

{% block title %} Liste des Users{% endblock %}

{% block body %}
<div class="container">
 
 {{form_start(form)}}
 <div class="row align-items-end">
 <div class="col">
{{ form_row(form.nom)}} 
 </div>
 <div class="col">
 <button type="submit" class="btn btn-success"> Rechercher</button>
 </div>
 
 
</div>
 {{form_end(form)}}
 
 
 

<h1> Users </h1>
<a href="{{ path('new_user')}}" class="btn btn-primary">ajouter un User</a>
 {%  if users   %}
 <table id="articles" class="table table-striped">
 <thead>
 <tr>
 <th>id</th>
 <th>Nom</th>
 <th>Prenom</th>
 <th> Email</th>
 <th> Catégorie</th>
 <th>Actions</th>
 </tr>

 </thead>
 <tbody>
 {% for user in users    %}
 <tr>
 <td> {{ user.id}}</td>
  <td> {{ user.nom}}</td>
   <td> {{ user.prenom}}</td>
    <td> {{ user.email}}</td>
     <td> {{ user.category.title}}</td>
 <td>
 <a href="/user/{{user.id}}" class="btn btn-dark" > Details</a>
 <a href="/user/edit/{{user.id}}" class="btn btn-success" >Modifier</a>
 <a href="/user/delete/{{user.id}}" class="btn btn-danger"  onclick="return confirm('Etes vous surs de 
 vouloir supprimer  cet user ?');">Supprimer</a>
 </td>
 </tr>
 {%  endfor  %}
 
 </tbody>
 
 </table>
 {{ knp_pagination_render(users)}}
  {%  else  %}
 {% endif %}

 

</div>

    
    
{% endblock %}
